[project]
name = "eee-ci-types"
version = "0.0.0"
requires-python = ">=3.12"
dependencies = [
    "datamodel-code-generator[http]",
    "ruff",
    "jsonschema",
    "pydantic",
    "pytest",
    "requests",
    "rich",
]
